{
  "buildCommand": "DEPLOY_TARGET=vercel pnpm exec astro build",
  "outputDirectory": ".vercel/output",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "astro",
  "regions": ["iad1"]
}
