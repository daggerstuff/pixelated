## 2026-04-09 - Test browser language detection with SSR fallback | Pattern: When `typeof window !== 'undefined'` is checked but `window.navigator` is missing, an explicit TypeError can be thrown. Testing this specific edge case requires simulating the global environment where `window` exists but `navigator` doesn't, effectively verifying the fallback logic against incomplete browser environments or specific mock behaviors. | Action: Ensure to explicitly simulate incomplete global objects (like `window = {}` without `navigator`) to catch potential TypeErrors in fallback routines.
## 2026-04-12 - Add test for Auth0 password grant try/catch | Pattern: Handle legacy mock configurations | Action: Always append isolated test cases to legacy mock configurations instead of rewriting entire unmaintainable mock files
## 2026-04-12 - Add test for Auth0 password grant try/catch | Pattern: Handle legacy mock configurations | Action: Always ensure mock definitions match real implementation objects completely, including nesting like .oauth or .users, and use vi.fn(function() { return this; }) for backward compatibility with class constructors in Vitest.
