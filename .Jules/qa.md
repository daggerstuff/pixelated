## 2025-04-09 - Test BroadcastChannel Error Handling in TabSyncManager | Pattern: Mocking global constructors in Vitest to simulate browser API failures | Action: When testing browser APIs like BroadcastChannel that are instantiated using `new`, you must mock the constructor itself (e.g., `class MockBroadcastChannel { constructor() { throw new Error(...) } }`) rather than just using `vi.fn().mockImplementation()`, which will throw "is not a constructor" errors in Vitest.
