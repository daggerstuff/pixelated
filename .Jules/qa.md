## YYYY-MM-DD - Test _encrypt_email in auth routes | Pattern: Missing module mocking in unit tests | Action: Use patch.dict on sys.modules or direct function import bypass for uninstalled FastAPI
## 2026-06-21 - API Key Validation Timeout Test | Pattern: Time Dependent Testing with vitest | Action: Used `vi.useFakeTimers()` along with `vi.advanceTimersByTimeAsync()` to properly fast-forward through timeouts instead of waiting for natural timeouts to complete, especially useful when racing promises or mocking promise rejections.
## 2025-02-13 - Add tests for same_tenant_or_super_admin dependency | Pattern: Testing dependency bypass logic | Action: Added a new test `test_same_tenant_or_super_admin_passes_for_super_admin` verifying that a super_admin can bypass tenant checks and added to `src/pe/tests/test_rbac.py`.
## 2026-06-21 - Test catch block for validateApiKey error | Pattern: Mocking immediate rejection to test error handling | Action: Added a test using mockValidateApiKey.mockRejectedValue to verify that validateApiKey errors are caught and correctly return a 401.
## 2026-06-21 - Fix dangling timer in test | Pattern: Fake timers to clear dangling timeouts | Action: Wrapped error path test using `vi.useFakeTimers()` to avoid having dangling 5000ms setTimeout lingering in the event loop after test finishes.
