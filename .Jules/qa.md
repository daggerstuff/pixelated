## 2026-05-30 - Mocking window.matchMedia in jsdom tests | Pattern: when testing DOM manipulation that relies on window.matchMedia in a jsdom Vitest environment, mock it with vi.stubGlobal or Object.defineProperty. | Action: Use `vi.stubGlobal('matchMedia', vi.fn().mockImplementation(...))` to ensure that DOM-modifying code doesn't throw `TypeError: window.matchMedia is not a function`.
