## 2026-06-26 - Missing Authorization Check (IDOR) | Vulnerability: Missing check on whether a memory exists before updating | Learning: In API routes using a generic update gateway, always verify the object exists (and that the user has access) before performing the update, otherwise attackers could potentially infer existence of objects or corrupt data. | Prevention: Add an existence check returning 404 before passing to update.
