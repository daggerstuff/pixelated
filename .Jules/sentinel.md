## 2025-06-21 - [Sanitize Input and Prefix Threat Queries] | Learning: When dynamically building queries (e.g. Redis MATCH patterns) from user input, blindly prepending string constants is dangerous. You must first ensure the input is evaluated as a primitive string, strip invalid characters, and also strip any pre-existing prefix matching the one being added to avoid double-prefixing logic errors. | Action: Updated `searchThreatData` in `threat-query-provider.ts` to strictly cast `patternMatch` to a string, strip any existing `threat:` prefix from the sanitized string, and format the final pattern securely.
