## 2026-06-25 - Insecure Direct Object Reference in Memory Update | Vulnerability: Missing object existence check before update (IDOR) | Learning: Always verify that the targeted resource exists and the user has permission to modify it before processing updates | Prevention: Implement an existence check (e.g. `if (!existing) return 404`) immediately after fetching the resource in update/delete handlers.
