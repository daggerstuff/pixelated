## 2024-10-24 - [Time Range Selector ARIA Properties] | Learning: [Standard `<button>` elements utilizing `onClick` handlers for toggle behavior shouldn't use `role="radio"` and `radiogroup`, as screen readers expect arrow-key navigation for those. Converting them to `role="group"` with toggle buttons using `aria-pressed` aligns with keyboard functionality (`Tab` and `Enter/Space`).] | Action: [Updated `TimeRangeSelector` to use `role="group"` and `aria-pressed` instead of radio/checked attributes.]
