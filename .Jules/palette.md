## 2024-06-20 - Unconditional Skip Links | Learning: Conditionally rendering skip links based on keydown/mousedown JS state tracking is an accessibility anti-pattern. Screen readers may miss the element if they don't trigger the specific tracked events. | Action: Replaced JS state tracking with CSS-based visibility using sr-only and focus:not-sr-only classes so the skip link remains persistently in the DOM
