## 2024-03-20 - Memoization vs Extraction | Learning: When pure helper functions do not depend on component state or props, hoisting them outside the component is a more effective optimization than wrapping them in useCallback, as it entirely avoids hook overhead and recreation on each render. | Action: Hoisted getSeverityColor and getStatusColor outside of CrisisSessionFlagsManager component.
