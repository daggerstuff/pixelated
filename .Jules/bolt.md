## 2026-04-09 - Optimize manual refresh analysis with bulk updates | Learning: N+1 queries in loops calling DB updates (findByIdAndUpdate) and sequential external services cause major performance bottlenecks. Using Promise.all for concurrent processing and MongoDB bulkWrite reduces O(N) database trips to O(1). | Action: Always use Promise.all to fetch concurrent operations inside loops and aggregate database writes via bulkWrite when updating multiple documents.
## 2026-04-10 - Memoize mapped component lists based on API search queries | Learning: Component properties that map out large sets of API response arrays into child DOM elements without memoization causes N component unmounts and remounts, which harms application responsiveness. | Action: Use useMemo to wrap expensive map operations like API response results arrays that generate large lists of child nodes, applying it conditionally by ensuring its dependencies array handles state properly.
