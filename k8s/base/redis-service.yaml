apiVersion: v1
kind: Service
metadata:
  name: pixelated-empathy-redis
  namespace: pixelated-empathy
spec:
  selector:
    app: pixelated-empathy-redis
  ports:
  - port: 6379
    targetPort: redis
    protocol: TCP
