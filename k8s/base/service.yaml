apiVersion: v1
kind: Service
metadata:
  name: pixelated-empathy-api
  namespace: pixelated-empathy
  labels:
    app: pixelated-empathy-api
spec:
  selector:
    app: pixelated-empathy-api
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP