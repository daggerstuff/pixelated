apiVersion: v1
kind: Secret
metadata:
  name: pixelated-empathy-secrets
  namespace: pixelated-empathy
type: Opaque
stringData:
  # NOTE: This file is a template only. Render concrete values via CI or
  # scripts/render-k8s-secrets.sh.
  DATABASE_URL: "postgresql://REPLACE_ME_DB_USER:REPLACE_ME_DB_PASSWORD@REPLACE_ME_DB_HOST:5432/REPLACE_ME_DB_NAME"
  REDIS_URL: "redis://REPLACE_ME_REDIS_HOST:6379/0"
  JWT_SECRET: "REPLACE_ME_JWT_SECRET"
  API_KEY: "REPLACE_ME_API_KEY"