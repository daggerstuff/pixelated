# ---------------------------------------------------------------------------
# DEPRECATED — superseded by external-secrets.yaml (PIX-4097)
#
# This static Secret manifest is retained for reference and CI fallback.
# Production deployments should use ExternalSecret CRDs instead:
#   kubectl apply -f k8s/base/secret-store.yaml
#   kubectl apply -f k8s/base/external-secrets.yaml
#
# To render for CI/testing:
#   scripts/render-k8s-secrets.sh > k8s/base/secrets.generated.yaml
# ---------------------------------------------------------------------------

apiVersion: v1
kind: Secret
metadata:
  name: pixelated-empathy-secrets
  namespace: pixelated-empathy
  annotations:
    deprecated: "true"
    replacement: "k8s/base/external-secrets.yaml"
    deprecated-at: "2026-07-23"
type: Opaque
stringData:
  # NOTE: This file is a template only. Render concrete values via CI or
  # scripts/render-k8s-secrets.sh.
  DATABASE_URL: "postgresql://REPLACE_ME_DB_USER:REPLACE_ME_DB_PASSWORD@REPLACE_ME_DB_HOST:5432/REPLACE_ME_DB_NAME"
  REDIS_URL: "redis://REPLACE_ME_REDIS_HOST:6379/0"
  REDIS_PASSWORD: "REPLACE_ME_REDIS_PASSWORD"
  JWT_SECRET: "REPLACE_ME_JWT_SECRET"
  API_KEY: "REPLACE_ME_API_KEY"
