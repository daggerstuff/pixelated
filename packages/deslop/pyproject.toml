[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "deslop"
version = "0.1.0"
description = "A generalized AI Corpus Deslopping Engine."
authors = [{name = "Pixelated Empathy"}]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
deslop = "deslop.cli:main"
