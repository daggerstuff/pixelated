<<<<<<< SEARCH
  beforeEach(async () => {
    // Setup in-memory MongoDB
    mongod = await MongoMemoryServer.create()

    // Setup mock Redis
    redis = new Redis()
=======
  beforeEach(async () => {
    // Setup in-memory MongoDB
    mongod = await MongoMemoryServer.create()

    // Setup mock Redis
    redis = new Redis()
  }, 30000)
>>>>>>> REPLACE
