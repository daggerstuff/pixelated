---
import Layout from '@/layouts/Layout.astro'
import { PortalShell, SchedulingWidget } from '@/components/ehr/portal'

export const prerender = false
---

<Layout title="Scheduling — Client Portal" description="Schedule and manage appointments">
  <PortalShell activeFeature="scheduling" client:load>
    <SchedulingWidget client:load />
  </PortalShell>
</Layout>
