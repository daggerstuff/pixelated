<div class="hero-stage">
  <div class="hero-stage__frame hero-stage__frame--left"></div>
  <div class="hero-stage__frame hero-stage__frame--right"></div>
  <slot />
</div>

<style>
  .hero-stage {
    position: relative;
    display: grid;
    gap: 1.55rem;
    padding: clamp(1.2rem, 2vw, 1.7rem);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1.5rem;
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.05),
        rgba(255, 255, 255, 0.012)
      ),
      rgba(9, 15, 16, 0.68);
    box-shadow: var(--shadow-floating);
    backdrop-filter: blur(18px);
    -webkit-backdrop-filter: blur(18px);
    overflow: clip;
  }

  .hero-stage::after {
    content: '';
    position: absolute;
    inset: 0.9rem;
    border: 1px solid rgba(255, 255, 255, 0.04);
    border-radius: 1rem;
    pointer-events: none;
  }

  .hero-stage__frame {
    position: absolute;
    inset: 0 auto 0 0;
    width: 2px;
    background: linear-gradient(
      180deg,
      rgba(143, 184, 162, 0.7),
      rgba(143, 184, 162, 0)
    );
    pointer-events: none;
  }

  .hero-stage__frame--left {
    left: clamp(0.7rem, 1.5vw, 1rem);
  }

  .hero-stage__frame--right {
    left: auto;
    right: clamp(0.7rem, 1.5vw, 1rem);
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.3),
      rgba(255, 255, 255, 0)
    );
  }

  @media (max-width: 1080px) {
    .hero-stage {
      gap: 1rem;
    }
  }

  @media (max-width: 768px) {
    .hero-stage {
      padding: 0.85rem;
    }
  }
</style>
