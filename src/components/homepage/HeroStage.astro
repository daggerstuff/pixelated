<div class="hero-stage">
  <div class="hero-stage__rule hero-stage__rule--top"></div>
  <div class="hero-stage__rule hero-stage__rule--bottom"></div>
  <div class="hero-stage__wash"></div>
  <slot />
</div>

<style>
  .hero-stage {
    position: relative;
    display: grid;
    gap: clamp(1.8rem, 3vw, 3rem);
    padding: clamp(1.1rem, 2vw, 1.5rem) 0 0;
    overflow: visible;
  }

  .hero-stage__rule,
  .hero-stage__wash {
    position: absolute;
    left: 0;
    right: 0;
    pointer-events: none;
  }

  .hero-stage__rule {
    height: 1px;
    background-image: linear-gradient(to right, rgba(253, 252, 252, 0.14) 1px, transparent 1px);
    background-size: 6px 1px;
    background-repeat: repeat-x;
    opacity: 0.9;
  }

  .hero-stage__rule--top {
    top: 0;
  }

  .hero-stage__rule--bottom {
    bottom: -0.6rem;
  }

  .hero-stage__wash {
    inset: 0;
    left: -1rem;
    right: -1rem;
    border-radius: 4px;
    background: linear-gradient(180deg, rgba(48, 44, 44, 0.16), rgba(48, 44, 44, 0.02));
    mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.9), transparent 92%);
    z-index: 0;
  }

  .hero-stage > * {
    position: relative;
    z-index: 1;
  }
</style>
