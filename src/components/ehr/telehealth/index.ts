/**
 * EHR Telehealth (F1.12)
 *
 * Native embedded telehealth: one-click start, auto note pre-population.
 * WebRTC-first, Zoom fallback. No separate login required.
 */

export {
  TelehealthSession,
  type TelehealthSessionProps,
} from './TelehealthSession'
export {
  PreCallCheck,
  type PreCallCheckProps,
  type DeviceCheckResult,
} from './PreCallCheck'
export {
  RecordingConsentGate,
  type RecordingConsentGateProps,
} from './RecordingConsentGate'
