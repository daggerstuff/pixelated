/**
 * EHR Billing — Claims Tracking (F1.10)
 *
 * Clearinghouse API integration (Change Healthcare).
 * Eligibility checks, claim status tracking, denial alerts, one-click resubmission.
 * NOT a billing engine — see ADR-003.
 */

export {}
