---
/**
 * Brutalist Hero Component
 * High-impact mission statement
 */
export interface Props {
  title?: string
  subtitle?: string
  primaryCTA?: { text: string; href: string }
  secondaryCTA?: { text: string; href: string }
  ctaPrimary?: { text: string; href: string }
  ctaSecondary?: { text: string; href: string }
  stats?: Array<{ value: string; label: string }>
}

const {
  title = 'AI Roleplay Training for Therapists',
  subtitle = 'Practice difficult clinical conversations with realistic AI patients, structured feedback, and repeatable supervision workflows.',
  primaryCTA,
  secondaryCTA,
  ctaPrimary,
  ctaSecondary,
  stats,
} = Astro.props

// Support both prop naming conventions
const primaryAction = primaryCTA ||
  ctaPrimary || { text: 'Enter the Gym', href: '/training' }
const secondaryAction = secondaryCTA ||
  ctaSecondary || { text: 'Explore Intelligence', href: '/ai-engine' }
---

<section
  class="hero relative flex items-center overflow-hidden"
  data-component="brutalist-hero"
>
  <!-- Background Layer -->
  <div class="absolute inset-0 z-0">
    <div class="absolute inset-0 bg-void"></div>
    <div class="hero-atmosphere absolute inset-0 opacity-80">
      <div class="hero-atmosphere__plane hero-atmosphere__plane--left"></div>
      <div class="hero-atmosphere__plane hero-atmosphere__plane--right"></div>
      <div class="hero-atmosphere__beam"></div>
      <div class="hero-atmosphere__glow hero-atmosphere__glow--left"></div>
      <div class="hero-atmosphere__glow hero-atmosphere__glow--right"></div>
      <div class="hero-atmosphere__grid"></div>
      <div class="hero-atmosphere__scanline"></div>
    </div>
    <!-- Grain/Noise Overlay -->
    <div
      class="absolute inset-0 opacity-[0.03] pointer-events-none"
      style="background: radial-gradient(circle at center, transparent 0%, rgba(0,0,0,0.1) 100%);"
    >
    </div>
    <!-- Radial Focal Glow -->
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(0,255,136,0.05)_0%,transparent_70%)]"
    >
    </div>
  </div>

  <div class="container relative z-10 hero-shell">
    <div class="hero-layout">
      <div class="hero-copy">
      <!-- Tag/Status -->
      <div
        class="inline-flex items-center gap-2 px-3 py-1 bg-accent-primary-glow border border-accent-primary-dim rounded-full mb-8 animate-pulse-soft"
      >
        <span class="w-2 h-2 rounded-full bg-accent-primary"></span>
        <span
          class="text-[10px] hero-kicker uppercase tracking-widest text-accent-primary"
          >AI Roleplay Platform // Therapist Training</span
        >
      </div>

      <h1
        class="font-display font-black text-6xl md:text-8xl leading-tight mb-8 tracking-tighter text-white"
      >
        {title}
      </h1>

      <p
        class="text-xl md:text-2xl text-secondary max-w-2xl mb-12 leading-relaxed font-light"
      >
        {subtitle}
      </p>

      <div class="flex flex-wrap gap-6 items-center">
        <a href={primaryAction.href} class="btn-premium group">
          <span class="relative z-10">{primaryAction.text}</span>
          <div
            class="absolute inset-0 bg-accent-primary transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left duration-normal"
          >
          </div>
        </a>

        <a
          href={secondaryAction.href}
          class="group hero-secondary-link flex items-center gap-2 text-white text-sm uppercase tracking-widest no-underline"
        >
          <span class="opacity-60 group-hover:opacity-100 transition-opacity"
            >{secondaryAction.text}</span
          >
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            class="transform group-hover:translate-x-2 transition-transform"
          >
            <path
              d="M5 12H19M19 12L13 6M19 12L13 18"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="square"></path>
          </svg>
        </a>
      </div>

      <!-- Stats Section -->
      {
        stats && stats.length > 0 && (
          <div
            class="stats-grid mt-16 pt-8"
            style="border-top: var(--border-primary);"
          >
            <div class="grid grid-cols-3 gap-8">
              {stats.map((stat) => (
                <div class="stat-item text-center md:text-left">
                  <span class="stat-value font-display font-black text-4xl md:text-5xl text-accent-primary block">
                    {stat.value}
                  </span>
                  <span class="stat-label mt-2 font-mono text-xs uppercase tracking-widest text-secondary block">
                  <span class="stat-label mt-2 text-xs uppercase tracking-widest text-secondary block">
                    {stat.label}
                  </span>
                </div>
              ))}
            </div>
          </div>
        )
      }
      </div>

      <aside class="hero-visual" aria-label="Pixelated Empathy brand visual">
        <div class="hero-visual__frame">
          <div class="hero-visual__meta">
            <span class="hero-visual__eyebrow">Pixelated Empathy</span>
            <span class="hero-visual__status">Live supervision stack</span>
          </div>

          <div class="hero-visual__image-shell">
            <img
              src="/favicon.svg"
              alt="Pixelated Empathy brand mark"
              class="hero-visual__image"
              width="192"
              height="192"
              loading="eager"
              decoding="async"
            />
          </div>

          <div class="hero-visual__readout">
            <div class="hero-visual__readout-item">
              <span class="hero-visual__label">Mode</span>
              <strong>Roleplay</strong>
            </div>
            <div class="hero-visual__readout-item">
              <span class="hero-visual__label">Feedback</span>
              <strong>Structured</strong>
            </div>
            <div class="hero-visual__readout-item">
              <span class="hero-visual__label">Replay</span>
              <strong>Repeatable</strong>
            </div>
          </div>
        </div>
      </aside>
    </div>
  </div>

  <!-- Perspective Grid Footer -->
  <div
    class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-void to-transparent"
  >
  </div>
</section>

<style>
  .hero {
    min-height: clamp(38rem, 84vh, 50rem);
    padding: clamp(6.5rem, 10vh, 8.5rem) 0 clamp(4rem, 7vh, 6rem);
    background:
      radial-gradient(circle at top, rgba(34, 197, 94, 0.08), transparent 36%),
      linear-gradient(
        180deg,
        rgba(2, 6, 23, 0.88) 0%,
        rgba(2, 6, 23, 0.97) 100%
      );
  }

  .hero-atmosphere {
    overflow: hidden;
  }

  .hero-atmosphere__plane,
  .hero-atmosphere__beam {
    position: absolute;
    inset: auto;
  }

  .hero-atmosphere__plane--left {
    top: -10%;
    left: -6%;
    width: min(52rem, 70vw);
    height: min(36rem, 54vw);
    background:
      linear-gradient(135deg, rgba(11, 15, 24, 0.9), rgba(8, 18, 24, 0.5));
    clip-path: polygon(0 0, 84% 0, 62% 100%, 0 100%);
    opacity: 0.86;
  }

  .hero-atmosphere__plane--right {
    right: -7%;
    bottom: -16%;
    width: min(34rem, 44vw);
    height: min(34rem, 44vw);
    background:
      linear-gradient(160deg, rgba(7, 24, 28, 0.92), rgba(4, 10, 22, 0.45));
    clip-path: polygon(22% 0, 100% 0, 100% 100%, 0 82%);
    opacity: 0.88;
  }

  .hero-atmosphere__beam {
    top: 14%;
    right: 16%;
    width: min(28rem, 34vw);
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(0, 255, 136, 0.7) 48%,
      rgba(255, 255, 255, 0.28) 100%
    );
    transform: rotate(-24deg);
    transform-origin: right center;
    opacity: 0.8;
    box-shadow: 0 0 18px rgba(0, 255, 136, 0.18);
  }

  .hero-atmosphere__glow {
    position: absolute;
    border-radius: 9999px;
    filter: blur(90px);
    opacity: 0.55;
    transform: translateZ(0);
  }

  .hero-atmosphere__glow--left {
    top: 8%;
    left: -8%;
    width: min(44rem, 60vw);
    height: min(44rem, 60vw);
    background: radial-gradient(
      circle,
      rgba(16, 185, 129, 0.24) 0%,
      rgba(16, 185, 129, 0.08) 42%,
      transparent 72%
    );
  }

  .hero-atmosphere__glow--right {
    right: -6%;
    bottom: 4%;
    width: min(40rem, 52vw);
    height: min(40rem, 52vw);
    background: radial-gradient(
      circle,
      rgba(56, 189, 248, 0.18) 0%,
      rgba(14, 165, 233, 0.08) 45%,
      transparent 74%
    );
  }

  .hero-atmosphere__grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(148, 163, 184, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(148, 163, 184, 0.1) 1px, transparent 1px);
    background-position: center center;
    background-size: 72px 72px;
    mask-image: radial-gradient(
      circle at center,
      rgba(0, 0, 0, 0.9),
      transparent 82%
    );
    opacity: 0.16;
  }

  .hero-atmosphere__scanline {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.05),
        transparent 22%,
        transparent 78%,
        rgba(255, 255, 255, 0.03)
      ),
      repeating-linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.035) 0,
        rgba(255, 255, 255, 0.035) 1px,
        transparent 1px,
        transparent 4px
      );
    opacity: 0.08;
    mix-blend-mode: soft-light;
  }

  .stark-outline {
    -webkit-text-stroke: 1px rgba(255, 255, 255, 0.3);
    color: transparent;
  }

  .btn-premium {
    position: relative;
    padding: 1.25rem 2.5rem;
    background: transparent;
    border: 1px solid var(--accent-primary);
    color: white;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    overflow: hidden;
    text-decoration: none;
    display: inline-block;
    transition: color 0.3s ease;
  }

  .btn-premium:hover {
    color: black;
  }

  .hero-shell {
    max-width: 66rem;
  }

  .hero-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.15fr) minmax(20rem, 24rem);
    gap: clamp(2rem, 4vw, 4rem);
    align-items: center;
  }

  .hero-copy {
    max-width: 52rem;
  }

  .hero-kicker,
  .hero-secondary-link,
  .stat-label {
    font-family: var(--font-display);
    font-weight: 600;
    letter-spacing: 0.08em;
  }

  .hero-secondary-link {
    letter-spacing: 0.12em;
  }

  .hero-visual {
    display: flex;
    justify-content: flex-end;
  }

  .hero-visual__frame {
    position: relative;
    width: min(100%, 24rem);
    padding: 1.5rem;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.04), rgba(255, 255, 255, 0.02));
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow:
      0 24px 60px rgba(0, 0, 0, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(18px);
  }

  .hero-visual__frame::before {
    content: "";
    position: absolute;
    inset: 0.85rem;
    border: 1px solid rgba(0, 255, 136, 0.18);
    pointer-events: none;
  }

  .hero-visual__meta,
  .hero-visual__readout {
    position: relative;
    z-index: 1;
  }

  .hero-visual__meta {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
  }

  .hero-visual__eyebrow,
  .hero-visual__status,
  .hero-visual__label {
    font-family: var(--font-display);
    font-size: 0.72rem;
    letter-spacing: 0.12em;
  }

  .hero-visual__eyebrow {
    color: var(--text-primary);
  }

  .hero-visual__status,
  .hero-visual__label {
    color: var(--text-tertiary);
  }

  .hero-visual__image-shell {
    position: relative;
    display: grid;
    place-items: center;
    aspect-ratio: 1;
    margin-bottom: 1.5rem;
    overflow: hidden;
    background:
      radial-gradient(circle at center, rgba(0, 255, 136, 0.12), transparent 58%),
      linear-gradient(180deg, rgba(255, 255, 255, 0.03), rgba(255, 255, 255, 0.01));
  }

  .hero-visual__image-shell::before,
  .hero-visual__image-shell::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .hero-visual__image-shell::before {
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.06) 1px, transparent 1px);
    background-size: 28px 28px;
    opacity: 0.26;
  }

  .hero-visual__image-shell::after {
    background: linear-gradient(135deg, transparent 0%, rgba(0, 255, 136, 0.08) 100%);
  }

  .hero-visual__image {
    position: relative;
    z-index: 1;
    width: min(68%, 13rem);
    height: auto;
    filter: drop-shadow(0 14px 30px rgba(0, 0, 0, 0.35));
  }

  .hero-visual__readout {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.85rem;
  }

  .hero-visual__readout-item {
    display: grid;
    gap: 0.3rem;
  }

  .hero-visual__readout-item strong {
    font-family: var(--font-display);
    font-size: 0.92rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      padding-top: 6rem;
      padding-bottom: 4rem;
    }

    .hero-atmosphere__plane--left {
      width: 92vw;
      height: 62vw;
      left: -18%;
      top: -4%;
    }

    .hero-atmosphere__plane--right {
      width: 65vw;
      height: 65vw;
      right: -18%;
      bottom: 2%;
    }

    .hero-atmosphere__beam {
      top: 22%;
      right: -4%;
      width: 58vw;
    }

    .hero-atmosphere__grid {
      background-size: 44px 44px;
      opacity: 0.11;
    }

    .hero-layout {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-shell,
    .hero-copy,
    .hero-visual,
    .hero-visual__frame {
      max-width: 100%;
    }

    .hero-visual {
      justify-content: stretch;
    }
  }
</style>
