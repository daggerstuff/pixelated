---
import BaseLayout from './BaseLayout.astro'

interface Props {
  title?: string
  description?: string
  fullWidthMain?: boolean
}

const { title, description, fullWidthMain = false } = Astro.props
---

<BaseLayout {title} {description} showBackLink={false} {fullWidthMain}>
  <slot />
</BaseLayout>
