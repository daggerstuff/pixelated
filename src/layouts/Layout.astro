---
import BaseLayout from './BaseLayout.astro'

interface Props {
  title?: string
  description?: string
  keywords?: string
  fullWidthMain?: boolean
}

const { title, description, keywords, fullWidthMain = false } = Astro.props
---

<BaseLayout {title} {description} {keywords} showBackLink={false} {fullWidthMain}>
  <slot />
</BaseLayout>
