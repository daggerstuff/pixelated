# Pinned constraints for CI to avoid ABI mismatches
# Keep these in sync with supported wheels on ubuntu-latest
# AUTO-SYNCED in CI from requirements.txt
# DO NOT edit manually — update requirements.txt (uv pip compile) and let CI regenerate.
numpy==2.4.1
pandas==3.0.0
