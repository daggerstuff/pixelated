/**
 * EHR Native — Consent State Rules (Phase 3, F3.3)
 *
 * Versioned JSON config files for state-by-state consent rules.
 * Keyed by state code (e.g., CA.json, NY.json).
 *
 * Each file defines:
 * - Treatment types requiring consent
 * - Consent validity period
 * - Minor consent rules
 * - Expiration rules
 * - Required fields
 *
 * Changes require legal review and are versioned via git.
 *
 * @see docs/adr/ADR-007-consent-state-rules.md
 */

export {}
