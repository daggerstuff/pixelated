models:
  compression: nvidia/nemotron-3-super-120b-a12b
  routing_oracle: nvidia/nemotron-3-super-120b-a12b
  evaluation: nvidia/nemotron-3.5-lightning-30b-a3b

thresholds:
  short_description_tokens: 40
  min_reference_tokens: 30
  max_feedback_iterations: 2
  max_restore_steps: 3

oracle:
  num_test_queries: 8
  num_distractors: 4
  include_adversarial: true

# Prefer .env (see .env.example). Env vars override these fields.
# api_key: sk-...
# api_base_url: https://api.openai.com/v1
# base_url: https://api.openai.com/v1   # alias for api_base_url
use_llm: True
