// Centralized font imports — all layouts import from here to ensure
// consistent font loading and avoid duplicate <style> injection.
// Instrument Serif carries the display voice; Instrument Sans Variable
// carries reading and interface text. Spline Sans Mono remains app-data
// utility.
import '@fontsource/instrument-serif'
import '@fontsource-variable/instrument-sans'
import '@fontsource-variable/spline-sans-mono'
