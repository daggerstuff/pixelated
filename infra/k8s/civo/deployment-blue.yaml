# Blue-green deployment for Civo — Blue slot (active by default)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pixelated-empathy-blue
  namespace: pixelated-empathy
  labels:
    app: pixelated-empathy
    slot: blue
spec:
  replicas: 2
  progressDeadlineSeconds: 600
  strategy:
    type: RollingUpdate
    rollingUpdate:
      # maxUnavailable must be >= 1: with 0 the rollout deadlocks on full
      # nodes (surge pod can't schedule until an old pod frees capacity,
      # old pod won't terminate until the surge pod is Ready).
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: pixelated-empathy
      slot: blue
  template:
    metadata:
      labels:
        app: pixelated-empathy
        slot: blue
    spec:
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: dockerhub-registry-credentials
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: pixelated-empathy
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          image: pixelatedempathy/api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4321
              protocol: TCP
            - name: https
              containerPort: 8443
              protocol: TCP
          envFrom:
            - configMapRef:
                name: pixelated-empathy-config
                optional: true
            - secretRef:
                name: pixelated-empathy-secrets
                optional: true
          volumeMounts:
            - name: cloudflare-tls
              mountPath: /etc/ssl/cloudflare
              readOnly: true
            - name: tmp
              mountPath: /tmp
          env:
            - name: PORT
              value: "4321"
            - name: HTTPS_PORT
              value: "8443"
            - name: HTTPS_CERT_PATH
              value: "/etc/ssl/cloudflare/tls.crt"
            - name: HTTPS_KEY_PATH
              value: "/etc/ssl/cloudflare/tls.key"
            - name: DEPLOYMENT_SLOT
              value: "blue"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          startupProbe:
            httpGet:
              path: /health
              port: http
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 18
          livenessProbe:
            httpGet:
              path: /health
              port: http
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: cloudflare-tls
          secret:
            secretName: cloudflare-origin-tls
            optional: true
        - name: tmp
          emptyDir: {}
