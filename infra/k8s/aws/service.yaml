apiVersion: v1
kind: Service
metadata:
  name: pixelated-empathy
  namespace: pixelated-empathy
  labels:
    app: pixelated-empathy
spec:
  type: ClusterIP
  selector:
    app: pixelated-empathy
    slot: blue
  ports:
    - name: http
      port: 80
      targetPort: 4321
      protocol: TCP
