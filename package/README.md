# IBM

### testing:

to test bobshell without bobify script you need to:

- set BOBSHELL_API_KEY
- set GEMINI_API_KEY to same value

### manual tests

- version update
- show changelog for new version
- show changelog after update
- test prompt with `@file`
- change mode and saves to settings
- check mcp settings
  - with env replacement (+ in sandbox)
  - 'alwaysAllow' should work
